// 

function suma(){// declaración de la función, que es lo que quiero que haga // tiene que tener los parentesis si o si, puede llevar algo a dentro o no
    console.log(1 + 5)// lleva el resultado a la consola
    let resultado = 1 + 5// se guarda el resultado en una variable
    return resultado
}

console.log(suma());
suma()// esta es la llamada a la función "invocación", para que se ejecute la función

let